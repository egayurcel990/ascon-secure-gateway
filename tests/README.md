# Tests

This folder is reserved for automated unit and integration tests.

Suggested future tests:

- ASCON encrypt/decrypt roundtrip
- Invalid authentication tag rejection
- AAD path mismatch rejection
- Replay attack rejection
- Secure login success
- Insecure endpoint disabled when `DEMO_MODE=false`
